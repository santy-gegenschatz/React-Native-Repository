import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    firstScreen__view: {
        backgroundColor: '#AFF2EF',
        width: 350,
        height: 300,
        justifyContent: 'center',
        borderRadius: 25
    },
    firstScreen__text: {
        textAlign: 'center'
    } //
})
